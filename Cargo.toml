[workspace]
resolver = "2"
members = ["verbalize", "verbalize-cli", "xtask"]

[workspace.package]
edition = "2021"
license = "MIT OR Apache-2.0"
repository = "https://github.com/relu/verbalize"

# The profile that 'dist' will build with
[profile.dist]
inherits = "release"
lto = "thin"
