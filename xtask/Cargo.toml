[package]
name = "xtask"
version = "0.0.0"
edition.workspace = true
publish = false

[dependencies]
serde_json = { version = "1", features = ["preserve_order"] }
verbalize = { path = "../verbalize" }
